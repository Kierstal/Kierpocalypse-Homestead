module KH
{
    /* Resident's Bedroll - the deployable that seeds a Homestead resident.
       Set it down on a floor inside a claimed Homestead, then place a resident
       on a nearby tile. See KH_ResidentPlacement.lua. Icon/world model reuse a
       vanilla sheet for now; can be swapped for a bespoke bedroll sprite later. */
    item ResidentBedroll
    {
        DisplayCategory = Camping,
        ItemType        = base:normal,
        Weight          = 1.5,
        Icon            = Sheet,
        DisplayName     = Resident's Bedroll,
        Tags            = base:KHResidentBedroll,
    }
}
