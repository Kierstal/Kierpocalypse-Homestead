# Wake-up note - 2026-05-29

You asked me to "do all the things" while you nap. Here's what shipped, what's safe-by-default, and what to playtest first.

## Files changed / added

| File | Change | Effect |
|---|---|---|
| `shared/Compat/KH_ModCompat.lua` | Added 5 SSR mod flags + ID mappings | Detection only - nothing fires |
| `shared/Core/KH_NPCRegistry.lua` | NEW - centralized recognized-NPC registry | Foundation for recruitment + Loner rework |
| `client/Traits/KH_Loner.lua` | Rework v0.0.2: scan unrecognized bandits | **Activation gated** - dormant until first recruit |
| `client/Player/KH_SpawnLocations.lua` | NEW - profession-themed spawn override | **Toggle-gated OFF** - inert until you enable |
| `client/Player/KH_StarterKits.lua` | parkranger entry: +Crowbar, +Pen, +Notebook | Active on next new-game ParkRanger |
| `media/sandbox-options.txt` | +2 toggles (EnableSpawnLocations, StartWithCompanion) | Both default OFF |
| `media/lua/shared/Translate/EN/Sandbox.json` | +4 strings for the new toggles | Labels render in sandbox UI |

## What's safe-by-default vs what activates

**Safe (no surprise on playtest):**
- `KH_Loner` rework - only scans bandits AFTER you have ≥1 recognized NPC. Until you build recruitment and use it, behavior is unchanged.
- `KH_SpawnLocations` - sandbox toggle is OFF, and the ParkRanger candidate list is empty anyway. No teleport.
- `KH_NPCRegistry` - sits dormant, exposes `KH.recognizeNPC()`, `KH.isRecognizedNPC()`, `KH.recognizedNPCCount()` etc. Nothing populates it yet.
- `StartWithCompanion` toggle - default OFF, no companion spawned.

**Active (changes you'll see):**
- ParkRanger starter kit now includes **Crowbar + Pen + Notebook** alongside existing Map + Whistle + Big Hiking Bag. The crowbar is blunt (no Fear-of-Blood penalty), doubles as KH_PryAction tool.
- SSR mods detected by KH_ModCompat. `[KH] ModCompat: detected ...` log line will include `ssr_core, ssr_quests, ssr_e1, ssr_e2, ssr_e3` if they're loaded.

## First playtest goals (priority order)

1. **Boot once and confirm clean load.** Watch console for `[KH] NPCRegistry v0.0.1 loaded`, `[KH] Loner trait behavior v0.0.2 loaded`, `[KH] SpawnLocations v0.0.1 loaded`. Any error means I broke something - share the stack.
2. **Check sandbox UI.** New Game → Sandbox → "KH Compatibility" page should show 8 toggles now: the 4 from before (Hygiene/Toilet/Recipes/BeefJerky), 2 from BanditsTame (TameBandits, TameBanditsWeekOne), and the 2 new ones (EnableSpawnLocations, StartWithCompanion). All readable labels, all reasonable tooltips on hover.
3. **Start a new game as ParkRanger.** Confirm you spawn with Crowbar + Pen + Notebook in inventory. Confirm console says no errors.
4. **Optional: try Loner trait verification.** If you don't recruit anyone, you should feel NO Loner stress even with bandits nearby (registry empty → activation gate not met). This is the same as before but the path is different now.

## What needs your input (next session)

1. **B42 HuntingRifle item ID** - I held off on adding it because I wasn't sure if it's `Base.HuntingRifle` or renamed in B42. If you spawn one in-game (debug menu) we can confirm and add to the ParkRanger kit.
2. **ParkRanger spawn coordinates** - the SpawnLocations skeleton needs real tile coords for ranger-themed buildings. With debug mode + coordinate display, walk to the ranger station / hunting lodge near Muldraugh and capture the X,Y of a tile inside the building. I'll add to LOCATIONS table and you can flip the toggle.
3. **Loner activation choice** - currently the trait activates on first recognition. Alternative: keep it gated behind a sandbox toggle (`KHCompat.LonerRecognizesResidents`) so you can A/B test. Let me know if you want the toggle path.
4. **Two-mode community design choice** - we sketched recruit-vs-deliver as a dialogue choice. Want to start building the dialogue injection into BWOChat next, or work on a different piece first?

## Architecture pieces still ahead

- **Recruitment flow** (BWOChat option injection → bandit Companion program → arrive home → SSR FNPC conversion). Biggest chunk of remaining work, probably 4-6 hours over multiple sessions.
- **Daily hunger scan** (per-homestead container check → SSR quest trigger when empty). 2-3 hours after recruitment ships.
- **Delivery-client mode** (track non-recruit NPCs' homes as delivery targets). 2-3 hours after hunger ships.
- **Trade integration with SSR e3** (some residents are also merchants). Optional, after the rest works.

## Memory state

Updated `MEMORY.md` index entries earlier today: FOOL Containers unsubbed, Bandits-taming paradigm, KH on-disk implementation log authoritative, KH Pets AE sibling decision. These should help a fresh instance orient quickly if you come back to this with someone different.

The `VILLAGE_DESIGN.md` spec remains the authoritative architecture doc. This wake-up note is just the session-specific status.

Have a good nap.
